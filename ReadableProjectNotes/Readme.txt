Project starter

https://github.com/udacity/reactnd-project-readable-starter.git

the ff does not work:
git clone 
https://github.com/udacity/reactnd-project-readable-starter.git Readable

for the server:
download the zip at the location above and then expand that in a folder called Readable
npm install
run this at the cmd line:
node server

run this: (if you have not don it before, do this first"
npm install -g create-react-app)
create-react-app  readable